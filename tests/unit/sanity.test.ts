import { describe, expect, it } from "vitest";

// Placeholder so `npm test` has something to run and CI wiring is verified
// before real content-schema tests land (see tests/README.md).
describe("scaffold sanity", () => {
  it("runs", () => {
    expect(1 + 1).toBe(2);
  });
});
