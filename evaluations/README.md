# evaluations/

Eval harnesses for any Claude-powered feature the app itself uses (as opposed to lesson content *about* evals, which lives under `content/domains/05-context-management-reliability/`).

Deliberately empty until a later phase — per `architecture/adr/0001-tech-stack.md`, no live Claude API usage ships in the app before Domain 4/5 are built, so there's nothing to evaluate yet. First real harness is tracked in `docs/roadmap.md` (V0.4).
