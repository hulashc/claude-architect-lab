# examples/

Standalone, runnable code snippets referenced from lesson content — e.g. a minimal agent loop, a tool schema definition, an MCP server skeleton. These are teaching artifacts, not part of the running app; each should be runnable on its own (`node examples/xyz.ts` or similar) with a comment header linking back to the lesson that references it.

Nothing here yet — first examples land alongside Domain 1 content.
